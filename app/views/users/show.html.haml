%h1= @user.name

= profile_image_tag(@user)
%p.profile_instructions Change profile image.

- if current_user == @user
  .upload_image
    = form_for @user do |f|
      = label_tag(nil, class: 'upload_wrapper') do
        %p Choose a new profile image.

        = f.file_field :image, required: true

        .button Upload File

        = f.submit('Update')
